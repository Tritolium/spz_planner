import styled from "styled-components"

export const StyledUserStats = styled.div`
    display: flex;
    flex-direction: column;

    max-height: 50vh;
    overflow-y: auto;

    td, th {
        padding: 0.5rem;
    }
`
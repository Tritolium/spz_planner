import styled from "styled-components";

export const StyledSettings = styled.div`
    form {
        display: flex;
        flex-direction: column;
        
        div {
            display: flex;
            align-items: center;
        }

        button{
            width: max-content;
        }
    }
`